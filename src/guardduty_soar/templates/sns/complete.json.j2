{
  "event_type": "playbook_completed",
  "playbook_name": "{{ playbook_name }}",
  "status_emoji": "{{ completion_details.final_status_emoji }}",
  "status_message": "{{ completion_details.final_status_message }}",
  "actions_summary": "{{ completion_details.actions_summary | replace('\n', '; ') }}",
  "finding": {
    "id": "{{ finding.Id }}",
    "type": "{{ finding.Type }}"
  },
  "resource": {{ resource.model_dump_json() | safe }}
}